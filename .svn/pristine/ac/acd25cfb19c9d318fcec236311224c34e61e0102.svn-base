$(document).ready(function() {
	 var icons = {
			 header: "ui-icon-circle-arrow-e",
			 activeHeader: "ui-icon-circle-arrow-s"
			 };
	$(".resultContentDiv1").accordion({
		icons: icons,
		collapsible: true
	});
	$(".resultContentDiv").accordion({
		icons: icons,
		collapsible: true
	});
	
	/*
	$( "#toggle" ).button().click(function() {
		if ( $( ".resultContentdiv" ).accordion( "option", "icons" ) ) {
			$( ".resultContentdiv" ).accordion( "option", "icons", null );
		} else {
			$( ".resultContentdiv" ).accordion( "option", "icons", icons );
		}
	});
	*/
});